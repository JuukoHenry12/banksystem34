spring.application.name=systemproject
server.port=8084

# Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/banksystem
spring.datasource.username=root
spring.datasource.password=Measure1234!@#$

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.hibernate.format_sql=true

# Use MySQL8Dialect for MySQL 8 or remove to auto-detect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.generate-ddl=true

# Optionally, configure HikariCP properties for connection pooling
# spring.datasource.hikari.maximum-pool-size=10
# spring.datasource.hikari.minimum-idle=5
